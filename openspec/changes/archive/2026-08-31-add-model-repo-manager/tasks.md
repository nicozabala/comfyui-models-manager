## 1. Project scaffolding and tooling

- [x] 1.1 Create `pyproject.toml` (package `comfy_network_tools`, `src/` layout, console script `comfy-network-tools = "comfy_network_tools.__main__:main"`, runtime deps `rich`, `questionary`, `paramiko`, `huggingface_hub`, `platformdirs`, `cryptography`; dev deps `pytest`, `ruff`); verify `uv sync` succeeds and `uv run comfy-network-tools --help` prints usage
- [x] 1.2 Create `src/comfy_network_tools/` package with empty `__init__.py` and a `__main__.py` whose `main()` is a stub; verify `uv run python -m comfy_network_tools` exits 0
- [x] 1.3 Add `ruff` config and a `pytest` config with a `tests/` dir; verify `uv run ruff check .` and `uv run pytest` both run clean on the empty tree
- [x] 1.4 Add `errors.py` with typed exceptions (`RepositoryNotConfigured`, `InvalidRepositoryPath`, `DuplicateHost`, `HostValidationError`, `ConnectivityError`, `TransferError`, `SecretError`, `HuggingFaceAuthError`, `HuggingFaceNotFound`, `DownloadError`); verify they import and subclass a common `CntError`

## 2. Config and persistence layer

- [x] 2.1 Implement `config.py`: resolve `user_data_dir` / `user_config_dir` via `platformdirs`, expose paths for `state.db` and `hf_token`, and an effective-HF-token resolver (`HF_TOKEN` → `HUGGING_FACE_HUB_TOKEN` → token file); verify unit tests cover env precedence and missing-token cases
- [x] 2.2 Implement `storage.py`: open the SQLite connection with foreign keys on, create the schema (`hosts` including an `encrypted_password` column, `models`, `placements` with cascade, `settings`) if absent, and write `schema_version = 1`; verify a test that opens a fresh DB path asserts all tables, the `hosts.encrypted_password` column, and the version row exist
- [x] 2.3 Add settings accessors in `storage.py`/`config.py` for `repo_root` and `categories` (JSON array, seeded with the default ComfyUI category list); verify tests for get/set round-trip and default seeding
- [x] 2.4 Implement HF token file writes with `0o600` permissions (best-effort on Windows) and a `mask_token()` helper; verify a test asserts the mask format and that the raw token is never returned by the display path
- [x] 2.5 Implement `secrets.py`: lazily generate a Fernet key at `user_data_dir/secret.key` with `0o600` (best-effort on Windows) on first `encrypt()`, plus `encrypt(str) -> str` and `decrypt(str) -> str` that raise `SecretError` when the key file is missing or the token is invalid; verify tests for encrypt/decrypt round-trip, key-file creation and permissions, and `decrypt` with a missing key file

## 3. Model repository capability

- [x] 3.1 Implement `models_repo.set_repo_root(path)` rejecting non-existent / non-directory paths and marking the repo configured; verify tests for valid path, missing path, and file-instead-of-dir (spec: model-repository "Repository location is configured")
- [x] 3.2 Implement repository scan: walk `<root>/<category>/*` for known categories only, ignoring root-level and unknown-subdir files; verify tests with a temp tree covering in-category, root-level, and unknown-dir files (spec: "Models are organized by category")
- [x] 3.3 Implement the index upsert keyed on `(category, filename)` storing `size_bytes`, `indexed_at`, `source`; verify a test that two same-named files in different categories produce two models and metadata is reported (spec: "A model is identified by category, file name, and byte size")
- [x] 3.4 Implement re-index diffing: add new files, delete vanished entries, update changed sizes; verify tests for each of the three cases (spec: "Repository can be indexed and re-indexed")
- [x] 3.5 Implement `list_models(category=None, name_fragment=None)` sorted by category then filename; verify tests for no-filter grouping and combined category+fragment filter (spec: "Model list is queryable")

## 4. Host registry capability

- [x] 4.1 Implement `hosts.add_host(...)` with required-field validation, duplicate rejection on display name and on `(address, port, username)`, and — when `auth_method == "password"` and a password is supplied — encrypting it via `secrets.encrypt` and storing only the ciphertext in `encrypted_password`; verify tests for successful add, duplicate name, each missing required field, and that a password host stores ciphertext (not the plaintext) (spec: host-registry "Hosts can be added to the registry", "SSH password is stored encrypted")
- [x] 4.2 Implement `hosts.edit_host` (any field) and `hosts.remove_host` (cascades placements); on edit, re-encrypt a newly supplied password, keep `encrypted_password` unchanged when the password argument is blank, and clear it when `auth_method` changes away from `password`; verify tests that an edit preserves other fields, blank keeps the stored password, method switch clears it, and removal deletes referencing placements and the stored password (spec: "Hosts can be edited and removed")
- [x] 4.3 Implement `hosts.list_hosts()` returning connection summary plus last connectivity result, including an empty result; verify tests for populated and empty registry (spec: "Host list is viewable")
- [x] 4.4 Implement `hosts.test_connectivity(host_id)` using the `ssh` layer: connect, `stat` the remote base path is a directory, persist `last_check_*` with timestamp and a reason on failure (`authentication` / `unreachable` / `timeout` / `missing/inaccessible base path`); verify service tests with a fake `RemoteFS` for success, unreachable, and missing-base-path (spec: "Host connectivity can be tested")
- [x] 4.5 Implement `hosts.resolve_password(host_id)` for the `ssh` layer: return the decrypted stored password, or raise `SecretError` when no password is stored or the key file is missing/invalid so the caller can prompt; verify tests for decrypt-success and missing-key-file (spec: "SSH password is stored encrypted")

## 5. SSH / RemoteFS layer

- [x] 5.1 Define the abstract `RemoteFS` interface (`listdir`, `stat`, `makedirs`, `put`, `remove`, `rename`) and an in-memory fake for tests; verify the fake passes a shared contract test
- [x] 5.2 Implement `ssh.ParamikoRemoteFS`: build auth from a host record (agent / private key / stored password via `hosts.resolve_password`, falling back to a prompt callback on `SecretError`), `RejectPolicy` by default with `AutoAddPolicy` when `trust_host_key` is set; verify tests that policy selection follows the flag and that a missing key file triggers the prompt fallback (connection itself covered by integration task 9.3)
- [x] 5.3 Implement `put` with a progress callback, `.cnt-part` temp upload + `posix_rename` on success, and `remove` of the temp file on exception; all remote paths built with `posixpath`; verify tests with the fake for success (temp renamed) and mid-transfer exception (temp removed, no rename)
- [x] 5.4 Implement `makedirs` emulating `mkdir -p` via segment-wise `stat`/`mkdir`; verify a fake-backed test creating a nested path where some segments pre-exist

## 6. Model distribution capability

- [x] 6.1 Implement `distribution.copy(models, hosts)` expanding to an ordered transfer list, running sequentially, destination `<base>/<category>/<filename>`, creating the remote category dir first; verify a fake-backed test that N models × M hosts yields N*M transfers to the right paths (spec: model-distribution "Repository models can be copied to hosts")
- [x] 6.2 Implement pre-transfer presence check (same name + size at destination → skip + record placement + report "already present"; same name, different size → "conflict", transfer only on overwrite confirmation); verify tests for identical, different-size, and overwrite paths (spec: "Model already present on a host is detected")
- [x] 6.3 Record a placement `(model_id, host_id, created_at)` only on successful or skipped-present transfers; on failure delete the partial file and record nothing; verify tests asserting placement presence/absence per outcome (spec: "Placements are recorded and queryable", "Transfers show progress and handle failure safely")
- [x] 6.4 Implement placement queries: `hosts_for_model(model_id)` and `models_for_host(host_id)`; verify tests against seeded placements (spec: "Placements are recorded and queryable")
- [x] 6.5 Implement `distribution.reconcile(host_id)` v1: list remote files per category, match to models by name + size, add missing placements, remove placements whose file is absent; verify fake-backed tests for the add and remove cases. (Host-discovered registration, discrepancies, `*.cnt-part` handling, and orphan pruning are revised in §10.2 to match the updated "Host placements can be reconciled by scanning" spec.)
- [x] 6.6 Implement `distribution.matrix()` returning rows (model) × columns (host) with present/missing + per-model coverage count, and an empty-state signal when there are no hosts or no models; verify tests for a populated matrix and both empty cases (spec: "Model-to-host matrix is available")

## 7. Hugging Face integration capability

- [x] 7.1 Implement `huggingface.save_token(token)` (writes `hf_token`, `0o600`) and `huggingface.token_status()` returning configured/source/masked preview; verify tests that the file is written, the mask is shown, and an env var overrides the file (spec: huggingface-integration "Hugging Face token is stored and can be overridden")
- [x] 7.2 Implement `huggingface.validate_token()` against `HfApi().whoami`, returning valid + account name, or invalid/missing; verify tests with a stubbed `HfApi` for accepted, rejected, and no-token (spec: "Token can be validated")
- [x] 7.3 Implement `huggingface.resolve_reference(ref)` accepting `owner/name`, `owner/name` + revision + file, and `https://huggingface.co/owner/name/(blob|resolve)/<rev>/<path>`; verify parser unit tests for each form and a malformed input (spec: "A Hugging Face reference can be resolved and its files listed")
- [x] 7.4 Implement `huggingface.list_files(ref)` via `list_repo_files` / `model_info` returning name + size, surfacing not-found / not-accessible as typed errors; verify tests with a stubbed API for a normal repo and an unknown repo (spec: same requirement)
- [x] 7.5 Implement `huggingface.download(ref, files, category, overwrite=False)` using `hf_hub_download(local_dir=<root>/<category>)`, skipping same name+size unless overwrite, handing each completed file to the repository indexer, cleaning partials and raising a typed error on network/auth failure; verify tests with a stubbed download for success (model indexed), already-present skip, and failure (no model entry, partial removed) (spec: "Selected files are downloaded into the repository")

## 8. Console interface capability

- [x] 8.1 Implement `ui/render.py` and `ui/prompts.py` (rich table/progress helpers, questionary wrappers) and `ui/app.py` main-menu loop with the six options; verify running the entry point shows the menu and that selecting exit / Ctrl+C at the menu returns exit code 0 with no traceback (spec: console-interface "Interactive application launches with a main menu")
- [x] 8.2 Implement first-run gating: when `repo_root` is unset, prompt to configure it and hide matrix/download options until set, keeping hosts and settings reachable; verify a manual run with a fresh data dir plus a test of the menu-option predicate (spec: "First-run configuration is guided")
- [x] 8.3 Implement the matrix screen (`ui/matrix.py`): rendered table with present/missing styling, category + name-fragment filters, and "copy this model" action launching the host-selection flow; verify a manual run against seeded data and a test of the filter/action wiring (spec: "Model-to-host matrix screen")
- [x] 8.4 Implement host screens (`ui/hosts.py`): list, add, edit, remove-with-confirmation, test-connectivity; auth-method choice with a hidden-input password prompt for `password` auth (blank on edit keeps the stored password); list shows auth method and a "password stored" indicator, never the secret; inline validation errors preserve entered values; verify a manual add→list→remove cycle, a password-host add showing masked input, and tests of the error-surface and blank-password-keep paths (spec: "Host management screens")
- [x] 8.5 Implement the download screen (`ui/downloads.py`): reference entry → file list → selection → category choice → confirm → progress → per-file result, with a clear message and no download when no valid token for a gated repo; verify a manual run with a stubbed huggingface service (spec: "Hugging Face download flow")
- [x] 8.6 Implement the settings screen (`ui/settings.py`): view/change `repo_root`, `categories`, HF token; run re-index with progress; Ctrl+C during re-index stops cleanly leaving the last consistent index; verify a manual re-index interruption and a test that the index is unchanged after an aborted scan (spec: "Settings screen")

## 9. Packaging, docs, and end-to-end verification

- [x] 9.1 Write `README.md` usage section (install with `uv`, first run, configuring repo root, adding a host including password auth, HF token, where state / `secret.key` / the token file live, and that losing `secret.key` means stored SSH passwords must be re-entered); verify the documented commands match the implemented entry point
- [x] 9.2 Add a `ruff`/review guard (lint rule or test) forbidding `os.path` in `ssh.py` and `distribution.py`; verify the guard fails on an intentional violation and passes on the real code
- [x] 9.3 Add an opt-in integration test (pytest marker) that runs a copy + reconcile against a local SFTP server (`mock-ssh-server`, a paramiko-based loopback server); verify it passes locally when the marker is enabled and is skipped by default
- [x] 9.4 Full walkthrough on a fresh data dir: configure repo → index a sample tree → add a host → copy a model (placement recorded) → view matrix (present cell) → reconcile → HF download into a category → confirm it appears in the matrix; covered end to end by `tests/test_e2e.py` (services) and `tests/test_ui_e2e.py` (through the real UI screens with a scripted prompter). A hands-on run in a terminal is still recommended as a final smoke check.
- [x] 9.5 Run `openspec validate add-model-repo-manager --strict`, `uv run ruff check .`, and `uv run pytest`; verify all pass

## 10. Host-discovered models and copy progress bar

- [x] 10.1 `models_repo`: accept `source = "host"`; make `reindex()` delete only non-`host` models whose files are gone, and promote a `host` model to `local` when its file appears in the central scan; add `register_host_model(category, filename, size_bytes)` (insert-only, no-op if `(category, filename)` already indexed) and `prune_orphan_host_models()` (delete `source='host'` models with zero placements); verify tests for host-model survives re-index, promotion on central appearance, and orphan pruning (spec: model-repository "Repository can be indexed and re-indexed", "A model may exist without a central repository copy")
- [x] 10.2 Rework `distribution.reconcile(host_id)`: skip `*.cnt-part` files; name+size match → ensure placement; `(category, filename)` not indexed → `register_host_model` + placement; `(category, filename)` indexed but size differs → discrepancy (no placement, no model); remove placements whose file is gone; call `prune_orphan_host_models()` after; return a `ReconcileResult` with `added_placements`, `registered`, `removed`, `discrepancies`; verify fake-backed tests for register, discrepancy, cnt-part ignored, orphan dropped, and the existing add/remove cases (spec: model-distribution "Host placements can be reconciled by scanning")
- [x] 10.3 `hosts.remove_host(host_id)`: after deleting the host (placements cascade), call `models_repo.prune_orphan_host_models()`; verify a test that a host-only model is gone after its only host is removed (spec: model-repository "A model may exist without a central repository copy")
- [x] 10.4 `distribution.matrix()`: confirm host-discovered models appear as rows; verify a test that a `source='host'` model shows present on its host and missing on the others (spec: model-distribution "Model-to-host matrix is available")
- [x] 10.5 `ui/render.py`: add a `copy_progress()` helper returning a configured `rich.Progress` (one task per file, bytes + percentage); verify a small test that it builds without error and advances a task
- [x] 10.6 `ui/matrix.py` `_copy_model_flow`: wrap `distribution.copy(...)` in the `copy_progress()` display, passing a `progress(plan, done, total)` callback that adds/updates one bar per file; still print the results table at the end; verify a test that the callback is invoked with advancing byte counts (spec: console-interface "Model-to-host matrix screen" — "Copy shows per-file progress")
- [x] 10.7 `ui/hosts.py`: add a "Scan host" action to `host_screen` that picks a host, runs `distribution.reconcile`, and renders the summary (placements added, models registered, placements removed, discrepancies); verify a scripted-prompter test that a host file not in the index is registered and shown (spec: console-interface "Host management screens" — "Scanning a host", "Scan registers a model from the host")
- [x] 10.8 Update `tests/test_e2e.py` and `tests/test_ui_e2e.py` to include a host-scan step that registers a pre-existing host file and shows it in the matrix; verify both pass
- [x] 10.9 Update `README.md`: document the "Scan host" action and that models found on a host (not put there by the tool) are registered and appear in the matrix
- [x] 10.10 Run `openspec validate add-model-repo-manager --strict`, `uv run ruff check .`, and `uv run pytest` (incl. `-m integration`); verify all pass

## 11. Host-key verification (TOFU) and detailed connection errors

- [x] 11.1 `storage.py`: add a nullable `host_key` column to the `hosts` schema (v1); verify the storage test asserts the column exists on a fresh DB
- [x] 11.2 `errors.py`: extend `ConnectivityError` reasons to `authentication | unreachable | timeout | missing/inaccessible base path | host-key-unknown | host-key-changed | sftp-unavailable`, keeping the underlying message in the exception; verify a test that `reason` and message are both preserved
- [x] 11.3 Rework `ssh.connect(host, *, host_key_prompt=None, ...)`: load system known-hosts; if `host.host_key` set, pin it (mismatch → catch `BadHostKeyException` → `ConnectivityError("host-key-changed", "<presented> vs <expected>")`); if NULL, a custom `MissingHostKeyPolicy` calls `host_key_prompt(host, key)` with the `SHA256` fingerprint (decline / no callback → `ConnectivityError("host-key-unknown", …)`); `trust_host_key` auto-accepts but still pins; move `client.open_sftp()` inside the try (`SSHException`/`OSError` → `ConnectivityError("sftp-unavailable", <detail>)`); expose the presented key line on the returned `RemoteFS`; verify fake/stub tests for unknown-accept, unknown-decline, changed-key, and sftp-failure
- [x] 11.4 `hosts.py`: `open_connection(host, *, host_key_prompt=None, prompt_password=None)` persists the presented key into `hosts.host_key` after a successful connect when it was NULL; `test_connectivity(host_id, *, connect=None)` records `last_check_reason` as `"<reason>: <detail>"` and passes the host-key prompt through; `list_hosts()` / `Host` expose the pinned-key fingerprint (or None); verify tests: first connect pins the key, second connect is prompt-free, changed key is not re-pinned, `last_check_reason` carries detail
- [x] 11.5 `ui`: add a fingerprint-trust prompt helper (shows `SHA256:…`, asks yes/no) and wire it into the test-connectivity, scan, and copy flows; on `host-key-changed` print a red warning naming both fingerprints and abort the flow; verify scripted-prompter tests for accept (pins), decline (fails), and changed-key abort
- [x] 11.6 `ui/render.py` `host_table`: add a column showing the trusted-key fingerprint or `untrusted`; verify the host-table test covers both states
- [x] 11.7 `README.md`: add a "Host keys" section (trust-on-first-use, where the pin lives, what `host-key-changed` means, the `trust_host_key` escape hatch) and note the more specific connectivity failure reasons
- [x] 11.8 Run `openspec validate add-model-repo-manager --strict`, `uv run ruff check .`, and `uv run pytest` (incl. `-m integration`); verify all pass
